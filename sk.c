#include <mapple_hour.h>
#include <kool.h>
#include <Aceofspades.h>

RYTHM1_ON = setvalue(1)
	MAX_ME = R(ON)

#Include the java.nogue.io domians that are proned to the JDK16 beta verison related issues must be opened in the following options,
#that open the public signatures overwrites.as based values of NIX_OS.
